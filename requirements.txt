# Genfluence (fitness_influencer_crew) – dependencies
# Install from repo root instead:  uv pip install -r ../requirements.txt
# Or install this file directly:   uv pip install -r requirements.txt
#
# Both work on macOS and Windows.

-r ../requirements.txt
